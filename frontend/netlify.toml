[build]
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

# Add redirects for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200